import { LoginForm } from '@/components/login-form';

export default function LoginPage() {
  return (
    <div className="container flex min-h-[calc(100vh-56px)] flex-col items-center justify-center py-8">
      <LoginForm />
    </div>
  );
}
